<div class="presser">
	<%= image_tag asset_path('handAnimation.png') %>
	<div></div>
</div>
<div id="pressed"></div>
<script type="text/javascript">
	var pressing = false;
	var transitionPropCt = 4; //How many css props are transitioning?
	var transitionCt = 0; //This gets incremented
	$(".presser").bind('mousedown touchstart', function(e){
		e.preventDefault();
		pressing = true;
		$(this).addClass("active");
		$("#pressed").append("<br>Fire Down "+transitionCt);
	}).bind('mouseup touchend', function(e){
		pressing = false;
		$(this).removeClass("active");
		$("#pressed").append("<br>Fire Up "+transitionCt);
	}).bind('webkitTransitionEnd', function(){
		transitionCt++;
		if((transitionCt%transitionPropCt)==1){
			if(pressing){
				$("#pressed").append("<br>Finished "+transitionCt);
			} else {
				$("#pressed").append("<br>Retreated "+transitionCt);
			}
		}
	});
</script>